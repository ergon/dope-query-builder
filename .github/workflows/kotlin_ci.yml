name: Kotlin CI

on:
    push:
        branches: [ "main" ]
    pull_request:
        branches: [ "main" ]

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout target branch
              uses: actions/checkout@v4
              with:
                  ref: main

            - name: status
              run: git status
            
            - name: Merge head branch into main
              run: git pull origin/${{ github.event.pull_request.head.ref }} --no-rebase --allow-unrelated-histories
            
            - name: Merge head branch into main
              run: git pull origin ${{ github.event.pull_request.head.ref }} --no-rebase --allow-unrelated-histories

            - name: Set up JDK 17
              uses: actions/setup-java@v4
              with:
                  java-version: '17'
                  distribution: 'adopt'
                  cache: gradle

            - name: Grant execute permission for gradlew
              run: chmod +x gradlew

            - name: Build with Gradle
              run: ./gradlew build

            - name: Run ktlint
              run: ./gradlew ktlintCheck

            - name: Run tests
              run: ./gradlew test
